<div class="mt-5 jumbotron">

  <h1 class = "font-weight-bold"><%= @pokemon.name.capitalize %></h1>

  <h6 class = "text-muted">Type: <%= @pokemon.species %></h3>

  <p class = "font-weight-light text-muted">Found on <%= @pokemon.found_on %> near <%= @pokemon.address %></p>

  <%# Remember with linktos, the string is the text of the link, and the second argument should always be the path to wherever you want the link to go, then you can add other elements such as classes, etc %>
  <%= link_to 'Go back home', pokemons_path, class: "btn btn-primary"%>
</div>
